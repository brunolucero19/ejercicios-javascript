let c=7

let f = (c*1.8) + 32

if (f>85){
    console.log(`Su temperatura pasada a F es ${f}, y es una temperatura alta`)
} else{
    if (f<32){
        console.log(`Su temperatura pasada a F es ${f}, y es una temperatura baja`)
    } else{
        console.log(`Su temperatura pasada a F es ${f}, y es una temperatura moderada`)
        }
    }


